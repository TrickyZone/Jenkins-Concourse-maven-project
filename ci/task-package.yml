---
platform: linux
image_resource:
    type: docker-image
    source: {repository: maven}

inputs:
   - name: source-code

outputs:
  - name: output-folder
    

run:
   path: source-code/ci/task-script.sh